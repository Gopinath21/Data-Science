strip(): method is like trim function in java . 
It will remove white spaces in a string. 

len()
upper()
lower()
replace()
split()
format() method to insert numbers into strings


List :
List is a collection which is ordered and changeable. Allows duplicate members.
Tuple is a collection which is ordered and unchangeable. Allows duplicate members.
Set is a collection which is unordered and unindexed. No duplicate members.
Dictionary is a collection which is unordered, changeable and indexed. No duplicate members.

index is helpful to get the data from index.
change:
listname[2]="value"

length:
len(listname)

add items:
append()
listname.append("value")

insert method:=

this use used to insert value at perticular position
thislist.insert(1,"orage")

Remove Item:

remove() method removes perticaul value from the list

pop:=
 method removes the specified index, (or the last item if index is not specified):


del:
The del keyword removes the specified index:

copy():
this is used to copy the a list to another list.

==========================================================================
append()	Adds an element at the end of the list
clear()	Removes all the elements from the list
copy()	Returns a copy of the list
count()	Returns the number of elements with the specified value
extend()	Add the elements of a list (or any iterable), to the end of the current list
index()	Returns the index of the first element with the specified value
insert()	Adds an element at the specified position
pop()	Removes the element at the specified position
remove()	Removes the item with the specified value
reverse()	Reverses the order of the list
sort()	Sorts the list
